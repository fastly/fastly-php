language: php

php:
  - 7.2
  - 7.3
  - 7.4

before_install:
  - composer self-update
  - composer validate

install:
  - composer install --prefer-source --no-interaction

script:
  - vendor/bin/phpspec run
